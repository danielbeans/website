---
import Container from "@components/Container.astro";
import Link from "@components/Link.astro";
import { SITE } from "@consts";
---

<header>
  <Container>
    <div class="flex flex-wrap gap-y-2 justify-between">
      <Link href="/" underline={false}>
        <div class="font-semibold">
          {SITE.NAME}
        </div>
      </Link>
      <nav class="flex gap-1">
        <Link external href="https://github.com/danielbeans"> github </Link>
        <span>
          {`/`}
        </span>
        <Link external href="https://www.linkedin.com/in/danielfwilliams/">
          linkedin
        </Link>
        <span>
          {`/`}
        </span>
        <Link external href={`mailto:${SITE.EMAIL}`}>
          {SITE.EMAIL}
        </Link>
      </nav>
    </div>
  </Container>
</header>
